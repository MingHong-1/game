# 本地游戏资源目录

只有登记在 `src/assets/AssetManifest.ts` 的资源才允许进入 Phaser 加载队列。当前 Manifest 没有启用任何正式资源，因此本目录中的空分类不会产生请求或 404。

规则：

- 所有路径相对于 `public/`，统一以 `assets/` 开头。
- 文件名使用小写 kebab-case，不包含空格和版本号后缀。
- 正式资源加入前必须记录来源、许可和商业使用范围。
- 禁止远程 CDN、其他商业游戏素材和未授权字体/音频。
- 不要用空 PNG、空音频或伪造资源占位；缺失时使用程序 fallback。
